{% extends '@WebProfiler/Profiler/layout.html.twig' %}

{% block toolbar %}
    {% set icon %}
        <img src="{{ path('triotech_adminer_css', {'type': 'images', 'asset': 'favicon.png'}) }}" align="Adminer" style="filter: grayscale(100%);" alt="Adminer" />
        <span class="sf-toolbar-value">Adminer</span>
    {% endset %}

    {% set text %}
        <div class="sf-toolbar-info-piece">
            <b>Actions</b>
            <a href="{{ path('triotech_adminer_home') }}" target="_blank">Open Adminer</a>
        </div>
    {% endset %}

    {{ include('@WebProfiler/Profiler/toolbar_item.html.twig', { link: false }) }}
{% endblock %}
